<%# <h1>トップページ</h1> %>
<%# <% @posts.each do |post| %> 
  <%# <%= post.content %> 
  <%# <%= post.created_at %> 
<%# <% end %> 



<%# <h1>トップページ</h1> %>
<h1>Top page</h1>

<%# 「新規投稿」という文字リンクを押すと/posts/newという下層にリンク %>
<%= link_to '新規投稿', '/posts/new' %>  

<%# 投稿された分だけ、投稿日時と投稿内容表示される %>
<% @posts.each do |post| %>
  <div class="post">
    <div class="post-date">
      投稿日時：<%= post.created_at %>
    </div>
    <div class="post-content">
      <%= post.content %>
    </div>
  </div>
<% end %>